import mypkg.NaturalNumber;
import org.junit.Test;

import static org.junit.Assert.assertTrue;

public class TestNaturalNumber {

    @Test
    public void testNaturalNumber_isEven() {
        //NaturalNumber<Integer> naturalNumber = new NaturalNumber<>(12);
        //todo: fix error -> java: valueOf(int) in java.lang.Integer is defined in an inaccessible class or interface
        //assertTrue(naturalNumber.isEven());
        //assertTrue(naturalNumber.getT() == 12);
    }
}
